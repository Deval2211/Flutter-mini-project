# MongoDB Connection (using a simple local MongoDB setup)
# For college/development purposes, we'll use a local MongoDB
MONGO_URI=mongodb://localhost:27017/flutter_auth_app

# JWT Secret for token signing (use a strong secret in production)
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# Server Port
PORT=5000